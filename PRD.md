# Product Requirements Document (PRD)

## 1. 프로젝트 개요
**Class-Board (동급생 익명 메모장)**는 같은 수업을 듣는 학생들이 불필요한 회원가입 없이 익명으로 메모를 남기고 소통할 수 있는 웹 기반 놀이터입니다. 직관적인 메모장 형태의 UI를 제공하며, 모든 UI 텍스트는 자연스러운 한국어(한글)로 제공됩니다.

## 2. 기능 요구사항
### 2.1 글 작성 및 조회
- **전역 목록 공유**: 접속한 모든 사용자는 별도의 권한 없이 기존에 작성된 모든 메모 목록을 즉시 볼 수 있어야 함.
- **전체 현황**: 상단에 "전체 글 개수"를 명확하게 표시.
- **직관적 글 작성**: 눈에 잘 띄는 "새 글 작성" 버튼 제공.
- **글 데이터 구조**: 글 작성 시 `제목`, `내용`, `비밀번호` 3가지 필드를 필수로 입력.

### 2.2 검색 기능
- **키워드 검색**: 상단 검색창에 키워드를 입력 시, 글의 '제목' 또는 '내용'에 해당 키워드가 포함된 글만 즉시 필터링하여 목록에 표시해야 함.

### 2.3 권한 및 수정/삭제
- **비밀번호 기반 권한 통제**: 메모 작성 시 사용자가 임의로 지정한 `비밀번호`를 입력하여 글을 수정하거나 삭제. 
- 올바른 비밀번호를 입력하지 않으면 해당 작업(수정/삭제) 불가. (Supabase DB에서 암호 비교)

## 3. 비기능 요구사항
- **반응형 디자인 (모바일 최적화)**: 데스크톱 및 모바일 환경에서 모두 쾌적하게 동작해야 함.
- **성능**: Next.js App Router를 활용해 서버사이드 렌더링 및 캐싱을 달성하여 빠른 초기 로딩 속도 유지.
- **보안**: Supabase RLS(Row Level Security)를 적용해 클라이언트 접근을 최소화하고, 안전하게 데이터를 보호. 비밀번호는 평문이 아닌 해시값을 대조하는 등 안정적인 방식을 권장. TypeScript strict 모드 적용. Any 사용 금지.

## 4. 데이터베이스 스키마 (Supabase PostgreSQL)
### Table: `posts`
- `id` (uuid, Primary Key)
- `title` (text, Not Null) - 메모의 제목
- `content` (text, Not Null) - 메모의 내용
- `password` (text, Not Null) - 글 수정/삭제 권한용 비밀번호
- `created_at` (timestamp with time zone, default `now()`)

*(Row Level Security 제어: 조회(SELECT)는 누구나 가능, 수정/삭제(UPDATE/DELETE)는 요청 비밀번호가 컬럼과 일치할 때만 허용하도록 Server Component 또는 Edge Functions 처리)*

## 5. API 명세 (Next.js Server Actions / Supabase Client)
Next.js의 Server Component 구조를 활용하고, Supabase에 직접 요청:
1. `GET /posts` : 메모 목록 가져오기 및 검색 키워드를 포함한 쿼리 처리
2. `POST /posts` : 새 메모 작성 (payload: `{ title, content, password }`)
3. `PUT /posts/:id` : 특정 메모 수정 (인증 로직: 입력받은 비밀번호 검증 후 Update)
4. `DELETE /posts/:id` : 특정 메모 삭제 (인증 로직: 입력받은 비밀번호 검증 후 Delete)

## 6. UI/UX 요구사항
- **직관적 메모장 스타일**: 깔끔하고 가벼운 메모장 콘셉트의 레이아웃 구성.
- **자연스러운 한글 UI**: `Search` -> `검색`, `Submit` -> `작성 완료` 등 모든 영문을 배제하고 한국어 사용.
- **사용자 친화적 피드백**: 글 작성이나 수정/삭제 완료 후 명확하고 부드러운 UI적 알림(토스트 메시지 등) 제공.

## 7. v2 업데이트 (Theme, Style, User Interaction)
- **전역 컬러 테마**: 3M 포스트잇에서 영감을 받은 따뜻하고 친근한 형태의 파스텔톤 노란색(`bg-yellow-50` 계열)을 전체 배경에 활용하여 "Wall"의 정체성을 강화.
- **포스트잇 색상 선택**: 작성자가 노랑, 파랑, 분홍, 초록, 보라 등 5가지 색상을 폼에서 직접 골라 본인만의 느낌을 표현할 수 있게 함.
- **인터랙티브 보드 (Draggable)**: 가장 최근 메모이더라도, 다른 메모들과 드래그앤드롭으로 물리적 순서를 바꿀 수 있도록 정렬 데이터(`sort_order`)와 UI(`dnd-kit`)를 업데이트함.
